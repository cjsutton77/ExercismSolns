#if !defined(HEXADECIMAL_H)
#define HEXADECIMAL_H
#include <string>
#include <algorithm>
#include <iostream>
namespace hexadecimal {
long convert(std::string);
long hexi(char);
}  // namespace hexadecimal

#endif // HEXADECIMAL_H